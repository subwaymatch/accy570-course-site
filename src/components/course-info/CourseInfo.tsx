import { motion } from 'framer-motion';

export default function CourseInfo() {
  return (
    <motion.div exit={{}} className="columns">
      <div className="column is-one-fourth">
        <h2>Instructor Information</h2>
      </div>
    </motion.div>
  );
}
